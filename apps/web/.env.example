# No environment variables are required yet.
# The dev server proxies /api/* to http://localhost:3000 (see vite.config.ts);
# the API client always calls the relative /api/v1 path, so nothing here
# needs to name the API's host or port.
